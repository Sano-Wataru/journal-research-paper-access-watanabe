---
description: 査読用データの配置方法とコメント作成時の運用ルール
alwaysApply: true
---

# 査読関連ルール

## ディレクトリ構成

- 原稿データと査読用データは必ず分離する。
- 査読用データは、プロジェクトルート直下に `review` ディレクトリを作成し、その配下にのみ配置する。
- `src` 配下に査読用ファイルやレビューコメント用ファイルは置かない。
- 査読に関係するあらゆるデータ（査読コメント、査読者への説明用補助資料など）は、すべて `review/` 以下にまとめる。

## コメントのラベル付け

- 提案内容や方法論自体の見直しが必要な「本質的な問題」を指摘するコメントには、必ず `[クリティカル]` ラベルを付ける。
- 軽微な表現修正やフォーマット調整など、方法論や提案内容の根幹に関わらないコメントには `[クリティカル]` を付けない。
- `[対応状況]` のようなステータス欄・進捗管理用の項目は作成しない・要求しない。

## コメントの単位（分割ルール）

- 同じ問題について指摘している場合は、該当箇所が複数個所にまたがっていても、**1つのコメント単位**としてまとめる。
- 同じ問題の原因が複数個所に散在している場合も、それらをひとまとめにして「同一問題」として扱う。
- 逆に、問題の性質が異なる場合は、同じ節・同じ段落内であってもコメントを分ける。

## 原因分析メモの書き方

### 原因の内容

- `[原因]` には、査読コメントが出た**論文側の事実**と、**修正が必要な箇所**を一緒に書く。
- `[該当箇所]` などの新しいラベルは作らない。
- 原因の本文は、論文が現在どう書いてあるか、何が欠けているか、どう誤読されるかを述べる。
- 査読コメントを和訳・言い換えしない。
- 査読者の要求（「〜せよ」「〜を明らかにせよ」「〜を議論せよ」）を原因に書かない。
- 「〜と判断された」「〜するように指摘された」「以下を問われている」など、査読者の To-do を原因に再掲しない。
- 原因分析が AI による草案である場合は、ラベルを `[原因（AI案）]` と明示しておく。

### 原因に書く箇所の選び方

- `[原因]` のあとに挙げる箇所は、すべて**修正が必要な箇所**である。
- 修正が必要でない箇所を、原因の箇所として挙げてはならない。
- コメントに言及されている記述を拾い出しただけでは、箇所にしない。
- 実験条件、表の数値、図など、問題の根拠にはなるが本文を直す必要がないものは、原因の本文に書いてよい。箇所としては挙げない。
- コメントの主題に触れているだけの文（手順の要約、定義の再掲、実験条件の列挙）は、欠けや誤読の本体でなければ箇所にしない。
- 原因の該当箇所は、関連記述の列挙ではない。本来そう書いてあるべきだった箇所、すなわちこれから修正する箇所だけを挙げる。
- 手法や主張が論文中で最初に登場する箇所を優先する。後段の繰り返しや周辺の手順説明は挙げない。
- `\deleted` / `deletedblock` に囲まれた文は削除予定なので、該当箇所として引用しない。残る本文（未削除または `\revised`）だけを引用する。

### 原因の書式

書式は次のとおりとする。ラベルは `[原因]` のみ。

````
[原因]
（論文側の事実と、欠け・誤読）

○節「…」subsection「…」△段落目
"引用文"
（その箇所が修正を要する理由の短い注記）
````

- 各コメントごとの原因分析では、必ず**該当箇所の構造情報**を明示する。
  - 少なくとも `section` 名（あるいは番号）、可能であれば `subsection` / `subsubsection` までを書く。
  - どの段落か分かるように「1段落目」「3段落目」などの形で段落も指定する。
- 上記の構造情報に加えて、**該当箇所の具体的な英文（または和文）を引用**する。
  - 例: `"We split the learning dataset into training data and test data at a ratio of 8:2."` のように、実際に論文中に現れる文をそのまま記載する。
  - 引用は、修正が必要な文そのものとする。直す必要のない周辺文は引用しない。
  - `\deleted` / `deletedblock` 内の文は引用しない。
  - 引用時のダブルクオーテーションは “ ” ではなく **"`"`（ASCII の二重引用符）** を用いる。

## 言語・トーン

- 査読コメントは**日本語**で記述する。
- 査読コメントのトーン・スタイルは**敬語でなくてよい**（です・ます調でなくてもよい）。
- 査読者宛ての正式な返信文は別途作成する前提とし、本ルールでは対象外とする。

