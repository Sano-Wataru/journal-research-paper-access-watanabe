%1st paragraph
%1評価方法
\deleted{The classification models trained using the augmented data from three subjects were evaluated using the test data from twenty-eight subjects.}
\revised{Models trained on augmented data from three subjects were evaluated on test data from twenty-eight subjects.}
%2精度計算
\deleted{Accuracy was utilized to evaluate the classification models, defined as}
\revised{Accuracy is defined as}
\begin{equation}
  \begin{split}
    \frac{TM+TP+TS+TT}{TM+TP+TS+TT+FM+FP+FS+FT},
  \end{split}
\end{equation}
\deleted{where $TM$ is the number of correctly classified point clouds that actually indicate the mouse operation, $TP$, $TS$, and $TT$ are the number of correctly classified point clouds that actually indicate the pad operation, the sitting still without moving, and the typing, respectively, and $FM$, $FP$, $FS$, and $FT$ are the number of point clouds that were not correctly classified.}
\revised{where $TM$, $TP$, $TS$, and $TT$ count correct classifications for mouse operation, pad operation, sitting still, and typing, and $FM$, $FP$, $FS$, and $FT$ count misclassifications.}
