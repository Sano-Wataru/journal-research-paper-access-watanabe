%1st paragraph
%1Methodologyに基づいた処理
\deleted{In our experiment, the proposed system was processed according to the methodology described in Section~\ref{sec:method}.}
\revised{Processing followed the methodology in Section~\ref{sec:method}.}
  %1-1トリミング
  \deleted{Two vertices of the region where a person is located were determined and then the point clouds were trimmed.}
  \revised{Point clouds were trimmed from manually determined person-region vertices.}

%2 特徴抽出
\deleted{The features were generated using the process explained in Section~\ref{sec:method}.}
\deleted{  %2-1 法線
  Specifically, the normal estimator from Open3D was used and normals were stored with the coordinate data in point cloud data files.}
\deleted{  %2-2 Dimensionality featureとProportion of variance
  The covariances were generated and then the dimensionality features and the proportion of variance were calculated.}
\deleted{  %2-3 FPFH
  The FPFH estimator from Open3D was utilized and the FPFH values were stored in a separate file from the coordinate data.}
\revised{Normals, dimensionality features, proportion of variance, and FPFH were computed with Open3D estimators.}

  %3クラスタリング
  \deleted{The data were clustered using the process explained in Section~\ref{sec:method}.}
  \deleted{    %3-1 DBSCAN
    Specifically, outliers and some clusters were removed after implementing DBSCAN.}
  \deleted{    %3-2 k-means
    Some clusters were also removed after performing k-means.}
  \revised{DBSCAN and k-means clustering removed outliers and selected clusters.}

  %4ダウンサンプリング
  \deleted{After the clustering and downsampling, the number of points in each point cloud was adjusted to 4,096, which is the default value of the Github repository \cite{pytorchpointnet++}.}
  \revised{Each point cloud was downsampled to 4,096 points \cite{pytorchpointnet++}.}

  %5正規化
  \deleted{Normalization was performed using different values for each point cloud dataset used for the training.}
  \revised{Training data were normalized per dataset.}

  %6Data augmentation
  \deleted{Data augmentation was performed on the normalized data.}
  \deleted{    %6-1 PointCutMixについて
    For data augmentation, PointCutmix was used\cite{ZHANG202258}.}
\begin{deletedblock}
        %6-1-1トレーニングデータの用意
        Three point clouds A, B, and C were prepared. 
        %6-1-2ベータ関数による値の設定
        The beta function was used to randomly determine a real number $\lambda$. 
        %6-1-3トレーニングデータAとBの処理
        Points from point cloud B were assigned to each point in point cloud A. 
            %6-1-3-1 2つの点群の各点の距離を計測し、一番近い点で置き換える
            For each point in A, the distance to all points in B was measured and the index of the closest point in B was assigned. 
            %6-1-3-2上位λ%の点を置き換える
            Then, the top $\lambda$\% of points in A, sorted by distance, were replaced with their assigned points from B. 
        %6-1-4処理済みのAとCで処理
        Points from point cloud C were assigned to each point in point cloud A. 
            %6-1-4-1
            For each point in A, the distance to all points in B was measured and the index of the closest point in C was assigned. 
            %6-1-4-2
            Then, the top $\lambda$\% of points in A, sorted by distance, were replaced with their assigned points from C. 
\end{deletedblock}
  \revised{PointCutMix augmentation \cite{ZHANG202258} replaced the top $\lambda$\% of nearest-neighbor-matched points in cloud A with points from clouds B and C, where $\lambda$ was drawn from a beta distribution.}

%2nd paragraph
%1トレーニングデータは、学習用と研修用で4:1に分ける
\deleted{The remaining data were split into 4:1 for the training and validation.}
\revised{Augmented data were split 4:1 for training and validation.}
%2PointNet++のグルーピングはMSGを使用
\deleted{Multi-scale grouping (MSG) was used to create groups in PointNet++, as based on prior methods \cite{pytorchpointnet++,PytorchPointnetPointnet2}.}
\revised{PointNet++ grouping used multi-scale grouping (MSG) \cite{pytorchpointnet++,PytorchPointnetPointnet2}.}
%3分類の出力
\deleted{An output containing the parameters of the classification was created after the training was completed.}
\revised{Trained classification parameters were saved after training.}
